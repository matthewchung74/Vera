# LiveKit Agents Framework with Google and Deepgram plugins
livekit-agents[google,deepgram]>=1.0.0

# Silero VAD plugin
livekit-plugins-silero>=1.0.0

# Utilities
python-dotenv>=1.0.0

# PDF text extraction for email attachments
pdfplumber>=0.10.0

# Redis for session storage (reconnection memory)
redis>=5.0.0
